import { MemberEditResolver } from './member-edit-resolver';

describe('MemberEditResolver', () => {
  it('should create an instance', () => {
    expect(new MemberEditResolver()).toBeTruthy();
  });
});
