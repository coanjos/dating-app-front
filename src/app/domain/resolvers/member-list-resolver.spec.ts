import { MemberListResolver } from './member-list-resolver';

describe('MemberListResolver', () => {
  it('should create an instance', () => {
    expect(new MemberListResolver()).toBeTruthy();
  });
});
